namespace FamilyBusiness.CityGen.Data
{
    /// <summary>Batch 11: narrative flavor for the chosen gang start (deterministic from selection context).</summary>
    public enum StartingGangSpawnProfile
    {
        CheapLodging = 0,
        WorkingClassFoothold = 1,
        EdgeSafeSpot = 2,
        BackRoomStart = 3,
        FallbackStart = 4
    }
}
