namespace FamilyBusiness.CityGen.Data
{
    /// <summary>
    /// Large-scale geographic / infrastructure features (macro planning only).
    /// </summary>
    public enum MacroFeatureKind
    {
        River = 0,
        Coastline = 1,
        RailCorridor = 2
    }
}
