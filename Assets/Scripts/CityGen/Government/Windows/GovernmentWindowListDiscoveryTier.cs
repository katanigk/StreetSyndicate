namespace FamilyBusiness.CityGen.Government.Windows
{
    /// <summary>Batch 13: coarse discovery tier for list styling (labels remain discovery-safe strings).</summary>
    public enum GovernmentWindowListDiscoveryTier
    {
        RumoredIntel = 0,
        KnownWithoutMapPin = 1,
        MapVisible = 2
    }
}
